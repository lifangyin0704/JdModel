<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bwie.com.jdmodel">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/NoTitle">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Web_vpActivity"
            android:theme="@style/NoTitle" />
        <activity
            android:name=".UserActivity"
            android:theme="@style/NoTitle" />
        <activity
            android:name=".ResActivity"
            android:theme="@style/NoTitle" />
        <activity
            android:name=".RegActivity"
            android:theme="@style/NoTitle" />
        <activity
            android:name=".UserResActivity"
            android:theme="@style/NoTitle" />
        <activity android:name=".NiChengActivity" />
        <activity android:name=".FlActivity" />
        <activity android:name=".Shop_XQ_Activity" />
        <activity
            android:name=".ScanActivity"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            android:theme="@style/zxing_CaptureTheme"
            android:windowSoftInputMode="stateAlwaysHidden"/>
    </application>

</manifest>